apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sPSP
metadata:
  name: pod-must-run-as-nonroot
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
    namespaces:
      - kube-system
      - argocd
      - monitoring
      - security
  parameters: {}
