apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "magento.fullname" . }}-php
  labels:
    {{- include "magento.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount.php }}
  selector:
    matchLabels:
      app: {{ include "magento.fullname" . }}-php
  template:
    metadata:
      labels:
        app: {{ include "magento.fullname" . }}-php
        {{- include "magento.labels" . | nindent 8 }}
    spec:
      {{- $storage := .Values.storage | default dict }}
      {{- $generated := $storage.generated | default dict }}
      {{- $varDi := $storage.varDi | default dict }}
      {{- $pubStatic := $storage.pubStatic | default dict }}
      containers:
        - name: php
          image: "{{ .Values.image.php.repository }}:{{ .Values.image.php.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "magento.fullname" . }}-env
            - secretRef:
                name: {{ include "magento.fullname" . }}-secrets
          {{- if or (or ($generated.enabled | default false) ($varDi.enabled | default false)) ($pubStatic.enabled | default false) }}
          volumeMounts:
            {{- if ($generated.enabled | default false) }}
            - name: generated
              mountPath: /var/www/html/generated
            {{- end }}
            {{- if ($varDi.enabled | default false) }}
            - name: vardi
              mountPath: /var/www/html/var/di
            {{- end }}
            {{- if ($pubStatic.enabled | default false) }}
            - name: pubstatic
              mountPath: /var/www/html/pub/static
            {{- end }}
          {{- end }}
          ports:
            - containerPort: 9000
          resources:
            {{- toYaml .Values.resources.php | nindent 12 }}
          livenessProbe:
            tcpSocket:
              port: 9000
            initialDelaySeconds: 30
          readinessProbe:
            tcpSocket:
              port: 9000
            initialDelaySeconds: 10
      {{- if or (or ($generated.enabled | default false) ($varDi.enabled | default false)) ($pubStatic.enabled | default false) }}
      volumes:
        {{- if ($generated.enabled | default false) }}
        - name: generated
          persistentVolumeClaim:
            claimName: {{ include "magento.fullname" . }}-generated
        {{- end }}
        {{- if ($varDi.enabled | default false) }}
        - name: vardi
          persistentVolumeClaim:
            claimName: {{ include "magento.fullname" . }}-vardi
        {{- end }}
        {{- if ($pubStatic.enabled | default false) }}
        - name: pubstatic
          persistentVolumeClaim:
            claimName: {{ include "magento.fullname" . }}-pubstatic
        {{- end }}
      {{- end }}
